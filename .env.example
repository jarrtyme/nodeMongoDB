# 环境配置示例文件
# 复制此文件为 .env 并修改相应的配置值

# 运行环境
NODE_ENV=development

# MongoDB 数据库配置
# 开发环境
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=clothing_inventory

# 生产环境 MongoDB 配置（二选一）
# 方式一：使用完整连接字符串
# MONGODB_URI=mongodb://username:password@host:port/database?authSource=admin

# 方式二：使用分离的配置项
# MONGODB_HOST=your-mongodb-host
# MONGODB_PORT=27017
# MONGODB_DATABASE=clothing_inventory
# MONGODB_USER=your-username
# MONGODB_PASSWORD=your-password
# MONGODB_AUTH_SOURCE=admin

# 文件访问基础 URL（用于生成完整的文件访问链接）
# 注意：现在后端会自动从请求中获取协议和主机，此配置仅作为后备方案
# 生产环境：使用 HTTPS 域名（不要包含 /api 路径）
FILE_BASE_URL=https://www.wdmlzffonline.top

# 开发环境：可以使用本地地址（可选，后端会自动从请求获取）
# FILE_BASE_URL=http://localhost:3000

# CORS 跨域配置
# 生产环境建议指定具体域名，开发环境可以使用 *
CORS_ORIGIN=*

# JWT 密钥（用于生成和验证 token）
# 生产环境必须修改为强随机字符串
JWT_SECRET=your-secret-key-change-in-production

# JWT 过期时间（可选）
JWT_EXPIRES_IN=24h

# 服务器端口（可选）
PORT=3000

# 文件上传配置（可选）
UPLOAD_MAX_SIZE=5242880
UPLOAD_MAX_FILES=10
